import { StyleSheet } from "react-native";

export const defaultStyles = StyleSheet.create({
  debug1: {
    borderWidth: 1,
    borderColor: "red",
  },
  debug2: {
    borderWidth: 1,
    borderColor: "green",
  },
});
